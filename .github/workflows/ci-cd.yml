name: CI-CD

on:
  push:
    branches:
      - main
      - phase-*
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            client-ui/package-lock.json
            admin-ui/package-lock.json

      - name: Restore backend
        run: dotnet restore backend/PRDF.Lms.sln --configfile backend/NuGet.Config

      - name: Build backend
        run: dotnet build backend/PRDF.Lms.sln --no-restore --configfile backend/NuGet.Config

      - name: Test backend
        run: dotnet test backend/PRDF.Lms.sln --no-build

      - name: Install client UI
        run: npm ci
        working-directory: client-ui

      - name: Build client UI
        run: npm run build
        working-directory: client-ui

      - name: Install admin UI
        run: npm ci
        working-directory: admin-ui

      - name: Build admin UI
        run: npm run build
        working-directory: admin-ui

  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy placeholder
        run: |
          echo "Deploy client UI/admin UI to Vercel/Netlify and backend to Azure Container Apps."
          echo "Use environment secrets documented in docs/runbook.md."
